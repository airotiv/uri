#include <stdio.h>

int main(){

	int i, i_m, f, f_m;
	
	scanf("%d%d%d%d", &i, &i_m, &f, &f_m);
	
	min_i = i * 60 + i_m; //horário inicial em minutos
	min_f = f * 60 + f_m; //horário final em minutos

	if (min_i == min_f){
		
	}
	return 0;
}
